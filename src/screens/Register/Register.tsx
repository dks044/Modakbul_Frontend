import Layout from '@/components/Layout';
import React from 'react';
import {Text} from 'react-native-paper';

const Register = () => {
  return (
    <Layout AppbarHeaderName="회원가입">
      <Text>회원가입</Text>
    </Layout>
  );
};

export default Register;
